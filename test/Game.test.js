/*
import Game from "../src/Game";

test("Test Game", () => {
  const game = Game();

  expect(game.putCoordinate("C4")).toBe("You hit CPU Ship 5");
  expect(game.putCoordinate("G6")).toBe("You hit Ship 5");
  // This was tested if J10 was only the one that hasn't been picked.
  expect(game.randomCoordinate()).toBe("J10");
});
*/
