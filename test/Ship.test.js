import Ship from "../src/Ship";

test("A Ship Object", () => {
  /*
  const shipCoordinate = ["A2", "A3", "A4"];
  const firstAttack = "A3";
  const secondAttack = "A2";
  const thirdAttack = "A4";

  const missedAttackCoordinate = "B3";

  const cruiserShip = Ship(shipCoordinate);

  expect(cruiserShip.length).toBe(3);
  expect(cruiserShip.hit(firstAttack)).toBe(2);
  expect(cruiserShip.isSunk()).toBe(
    `You still have ${2} ships coordinates left.`
  );
  expect(cruiserShip.getShipHits()).toBe(1);
  expect(cruiserShip.hit(missedAttackCoordinate)).toBe("You miss!");
  expect(cruiserShip.hit(secondAttack)).toBe(1);
  expect(cruiserShip.getShipHits()).toBe(2);
  expect(cruiserShip.hit(thirdAttack)).toBe(0);
  expect(cruiserShip.isSunk()).toBe("The ship has sunk!!");
  */
});
