import Player from "../src/Player";
import Gameboard from "../src/Gameboard";
test("Player Object", () => {
  /*
  const myPlayer = Player([
    ["A2", "B2", "C2", "D2", "E2"],
    ["F1", "F2", "F3", "F4"],
    ["H10", "I10", "J10"],
    ["D8", "D9", "D10"],
    ["G6", "H6"],
  ]);

  const gameboard = Gameboard(myPlayer);

  const playerCoordinates = gameboard.getPlayerCoordinates();

  expect(myPlayer.getPlayerTurn()).toBe(true);
  myPlayer.changePlayerTurn(false);
  expect(myPlayer.getPlayerTurn()).toBe(false);
  expect(myPlayer.getCoordinates()).toEqual([
    ["A2", "B2", "C2", "D2", "E2"],
    ["F1", "F2", "F3", "F4"],
    ["H10", "I10", "J10"],
    ["D8", "D9", "D10"],
    ["G6", "H6"],
  ]);
  expect(playerCoordinates[0]).toEqual(["A2", "B2", "C2", "D2", "E2"]);
  expect(playerCoordinates[1]).toEqual(["F1", "F2", "F3", "F4"]);
  expect(gameboard.playerAllShipSunk()).toBe(false);

  expect(gameboard.receivedAttack("G6")).toBe("You hit Ship 5");
  expect(gameboard.receivedAttack("H6")).toBe("You hit Ship 5");
  */
});
